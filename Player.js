"use strict";

const {AI} = require("./AI");
const
    class Player {
        constructor(name, score, gestures) {
            this.name = name            // ! done
            this.score = 0;              
            this.selectedGesture = []; // ! done
            this.gestures = ["Rock", "Paper", "Scissors", "Lizard", "Spock"]; // ! done
}

}

module.exports = {
    Player: Player,
}